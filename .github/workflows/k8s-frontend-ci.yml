name: K8s Dashboard - Frontend CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'cicd-projects/02-k8s-health-dashboard/frontend/**'
      - '.github/workflows/k8s-frontend-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'cicd-projects/02-k8s-health-dashboard/frontend/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: cicd-projects/02-k8s-health-dashboard/frontend

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: cicd-projects/02-k8s-health-dashboard/frontend/package-lock.json

    - name: 📦 Install dependencies
      run: npm ci

    - name: 🏗️ Build React app
      run: npm run build

    - name: Check Docker Hub secrets (push only)
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        if [ -z "${{ secrets.DOCKERHUB_USERNAME }}" ] || [ -z "${{ secrets.DOCKERHUB_TOKEN }}" ]; then
          echo "::error::Missing Docker Hub secrets: DOCKERHUB_USERNAME / DOCKERHUB_TOKEN"
          exit 1
        fi

    - name: 🔐 Login to Docker Hub
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: 🐳 Build and push Docker image
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: docker/build-push-action@v5
      with:
        context: cicd-projects/02-k8s-health-dashboard/frontend
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/k8s-dashboard-frontend:latest
          ${{ secrets.DOCKERHUB_USERNAME }}/k8s-dashboard-frontend:${{ github.sha }}

    - name: ✅ Build complete
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        echo "✅ Frontend image built and pushed successfully!"
        echo "📦 Image: ${{ secrets.DOCKERHUB_USERNAME }}/k8s-dashboard-frontend:latest"
